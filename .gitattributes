# 줄바꿈 정규화 — 저장소 안에서는 LF 로 통일한다.
* text=auto

# Windows 에서 실행하는 스크립트는 CRLF 로 내려받는다.
*.ps1 text eol=crlf
*.bat text eol=crlf

# 이진 파일은 건드리지 않는다.
*.png  binary
*.woff2 binary
*.npy  binary
*.faiss binary
